# Status

## Current

`main` 分支正在准备 0.5.1：应用功能延续 0.5.0，公共许可证已切换为 PolyForm Noncommercial 1.0.0。已发布的 0.5.0 Release 保持 MIT License，不受追溯影响。

## Done

- 确认产品愿景与 MVP 范围
- 将窄窗基准升级为 340 × 720 px，紧凑与长票档分别为 320 × 620 和 380 × 820
- 确认“风格展示板＋电影票根＋热敏纸”的视觉方向
- 确认公共词典与个人风格分离
- 建立架构和关键决策记录
- 建立 35 个结构化首批词条
- 完成票根式图鉴、详情、搜索与实时 CSS 样张
- 完成 Lab 权重、规则冲突、配方生成与本地保存
- 建立 GitHub 贡献、安全、Issue 与 Pull Request 模板
- 通过 JavaScript 语法检查；12 个词条 ID 唯一且必填字段完整
- 修正列表样张侵入文字区的问题
- 将列表票根改为真实裁切缺口与整体异形阴影
- 曾为“我的风格”探索例图、Figma、Skill 与手动四种入口，随后精简为例图提取与文字录入
- 建立低成本视觉解析、文本整理与 API Key 安全方案
- 修正票根阴影只作用于纸张本体，恢复缺口内侧弧线
- 固定三页底部导航位置
- 顶部右侧改为设置，并加入窗口、置顶与 AI 网关配置；封装前移除仅供开发讨论的最终横测区
- 使用明确 Grid 区域彻底固定三页底部导航
- 将票纸改为不透明实体，避免整票阴影透进内容区
- 统一缺口裁切与内弧坐标
- 增加网页风格收录、分类管理和词条排序
- 搜索升级为别名、近似拼写与多关键词匹配
- 收录表单精简为网址必填、AI 备注选填
- 分类条支持展开收起，新增分类改为图鉴页弹窗
- 词条详情可直接调整所属分类
- 图鉴票根支持长按拖动排序
- Lab 限制最多 4 种并强制总权重为 100%
- 权重滑块把手改为复古红色
- Lab 滑块改为互不牵动的自由份量，实际百分比由后台实时归一化
- 分类展开和新增按钮缩至与分类标签同高
- 补入 Apple 设计时间轴：Aqua、拉丝金属、早期 iOS 拟物、iOS 7、Big Sur、Liquid Glass
- 补入经典设计流派：包豪斯、瑞士国际主义、现代极简、装饰艺术、孟菲斯、中世纪现代、构成主义、极繁主义与 Frutiger Aero
- 增加“当代产品案例”分类，以带日期和来源的快照收录 Vercel Geist、Kimi K3、Claude、ChatGPT 与 Figma
- 为全部 35 张票根提供独立 CSS 视觉小样，并通过脚本语法与样张映射检查
- 修复长按排序：票根随指针移动、跨票根持续追踪、红线标记落点，松手后保存
- 增加 Shift＋↑/↓ 键盘排序入口
- 默认馆藏按“风格体系 → 当代产品案例 → 时代与平台 → 布局 → 技法 → 组件”排列
- 将单张票根与 Lab 配方升级为结构化完整 UI Prompt，覆盖页面内容、布局、字体、色彩、组件、状态、响应式和无障碍
- 在提示词标题旁增加一键复制按钮
- 将像素词条明确为“8-bit 像素界面”，补充 8-bit、Pixel Art UI 等检索别名
- 增加古希腊装饰语汇、新古典主义、拜占庭马赛克、哥特式彩饰手抄本、巴洛克和洛可可
- 增加无第三方依赖的本地 AI 网关，统一适配千问、DeepSeek 与 Kimi 的 OpenAI 兼容接口
- 设置页可连接 Key、查看保存状态并执行最小烟测；浏览器开发版使用进程内存，桌面版使用 Windows 加密存储并在启动时恢复
- “我的风格”例图会在本机缩放后交给视觉模型；文字规则可直接保存，也可主动交给文本模型整理
- AI 输出统一为命名、摘要、色彩、字体、布局、材质、组件、动效、保留项、禁用项、证据与置信度
- 生成可编辑并一键复制的中英文完整 UI Prompt，保存时保留结构化分析结果
- 完成全界面字号审计：实际阅读与操作文字最低 10px，正文提升到 11–13px；微缩字号只保留在风格样张内部
- 将例图分析从“界面转述”改为“跨产品风格迁移”：隔离原图业务内容、Tab 名称、具体图标和信息架构，只提取可复用的视觉系统
- 接入“千问看图提取证据 → DeepSeek 删除来源内容并生成最终 Prompt”的两段式链路
- 将网关烟测拆为视觉路线与文本路线，并在结果中展示实际模型流水线
- 增加 AI 结果完整性校验、嵌套 JSON 兼容与“只重试文本复审”，避免空 Prompt 被误判为成功或重复支付视觉解析费用
- 将 DeepSeek 的无效 JSON、超时与接口错误纳入可恢复审校状态，并优先使用严格 JSON 输出模式
- 将 DeepSeek 输出改为紧凑审校补丁并由网关合并千问证据，降低长双语 JSON 截断与空响应概率
- 停止把 DeepSeek 作为默认审校器，改用同一枚千问 Key 完成不含图片的第二次文本复审；DeepSeek 仅保留为可选备用
- 个人风格例图收录时自动生成仅存本机的压缩封面；Lab 配方自动用参与风格的小样组成封面
- 私人馆藏统一为可点击的票根列表，新增个人风格详情页，可回看来源、风格 DNA、配方和中英文 Prompt
- 个人风格详情右上角提供编辑与删除；编辑可更换封面、名称、说明和 Prompt，删除包含二次确认
- 精简全局说明文字，移除最终横测、无效预算和未开放自定义供应商等开发态设置
- 将网页风格收录改为诚实的“来源收集”，保存为待整理票根，不再伪装成尚未实现的 AI 自动读取
- 完成 Electron 桌面壳：透明无边框票根窗口、拖动、最小化/关闭、置顶、尺寸切换与单实例
- 将本地 AI 网关内置到桌面主进程，并使用自动空闲端口避免与网页开发网关冲突
- 将桌面版 API Key 升级为 Windows 用户级加密存储；只需首次填写，之后自动恢复且不进入仓库
- 顶部设置印章改为常规齿轮图标，提高入口识别性
- 增加正式票根 `SS` 应用图标与小尺寸托盘圆章，Windows 构建不再使用 Electron 默认图标
- 增加系统托盘菜单：显示/隐藏、始终置顶、贴左、贴右与退出；窗口 `×` 改为隐藏到托盘
- 增加多显示器安全的位置记忆与左右贴边吸附，重新启动后恢复窗口位置和吸附方向
- 0.5.0 首发采用 MIT License，并同步更新 npm 包许可元数据
- 当前源码与后续版本改用 PolyForm Noncommercial 1.0.0，商业使用需要版权所有者另行书面许可

## Next

- 发布采用新许可证的下一版 Windows 免安装便携版
- 使用同一组四张截图继续验证千问自复审与三档调用强度
- Kimi 上游恢复后，以同一组截图和文字规则补做横测
- 为当代产品案例增加来源、快照日期与更新提示
- 后续评估代码签名安装包；当前先提供免安装便携版

## Blocked

- Kimi Open Platform 当前返回引擎过载；DeepSeek 长 JSON 审校不稳定。两者均不再阻塞千问单供应商主链路。
